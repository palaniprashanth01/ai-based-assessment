[build]
  base = "frontend"
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[functions]
  node_bundler = "esbuild"

[functions."app/api/assess/route"]
  # Gemini PDF calls can take 20-50s on large docs.
  timeout = 60
